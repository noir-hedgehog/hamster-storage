# 前后端集成指南

## 快速开始

### 1. 启动后端服务

```bash
cd Backend
npm install
npm run dev
```

后端将在 `http://localhost:3001` 启动

### 2. 配置前端环境变量

```bash
cd Frontend
cp .env.example .env
```

编辑 `.env` 文件，确保API地址正确：
```
VITE_API_BASE_URL=http://localhost:3001/api/v1
```

### 3. 启动前端服务

```bash
cd Frontend
npm install
npm run dev
```

前端将在 `http://localhost:3000` 启动

## API集成说明

### API服务层

前端使用 `src/services/api.ts` 中的 `apiClient` 来调用后端API。

所有API调用都是异步的，返回Promise。

### 数据流

1. **组件** → 调用 `useStorage()` hook
2. **StorageContext** → 调用 `apiClient` 方法
3. **API Client** → 发送HTTP请求到后端
4. **后端API** → 处理请求，返回数据
5. **StorageContext** → 更新状态
6. **组件** → 重新渲染

### 错误处理

- API调用失败时会显示错误信息
- 网络错误会显示重试按钮
- 所有错误都会记录到控制台

### 加载状态

- 初始加载时显示加载动画
- 数据加载完成后显示内容
- 操作过程中可以显示加载状态

## 移动端优化

### 响应式设计

- **小屏幕 (< 640px)**: 
  - 侧边栏默认隐藏，通过菜单按钮打开
  - 工具栏按钮只显示图标
  - 卡片布局改为单列

- **中等屏幕 (640px - 1024px)**:
  - 侧边栏可以切换显示/隐藏
  - 部分按钮显示文字
  - 卡片布局2列

- **大屏幕 (> 1024px)**:
  - 侧边栏始终显示
  - 所有按钮显示完整文字
  - 卡片布局3-4列

### 触摸优化

- 按钮和可点击元素有足够大的触摸区域（至少44x44px）
- 使用 `touch-action` CSS属性优化触摸响应
- 避免使用hover效果在移动端

### 性能优化

- 使用懒加载减少初始加载时间
- 图片使用适当的尺寸
- 减少不必要的重新渲染

## 常见问题

### CORS错误

如果遇到CORS错误，检查：
1. 后端 `.env` 中的 `CORS_ORIGIN` 设置
2. 前端访问的URL是否正确

### 连接失败

如果前端无法连接后端：
1. 确认后端服务正在运行
2. 检查端口是否被占用
3. 检查防火墙设置
4. 查看浏览器控制台的错误信息

### 数据不同步

如果数据没有同步：
1. 检查网络请求是否成功
2. 查看浏览器开发者工具的网络标签
3. 检查后端日志

## 开发建议

1. **使用浏览器开发者工具**: 查看网络请求和响应
2. **查看后端日志**: 了解API调用情况
3. **测试不同设备**: 确保响应式设计正常工作
4. **测试网络错误**: 模拟网络故障情况
